# Optional. Copy to .env for local runs. Do not commit .env.
# The Groq model name and API key are entered in the web page, not here.

# Prefill the model name box in the page (users can still change it).
# GROQ_MODEL=name-of-a-model-from-the-groq-console
